1-> pip install PyQt5
2-> pip install pyinstaller

3-> install the .exe file I"ve given with default settings
                               or
    go to "codecguide.com",
    click 'Download' button under 'K-Lite Codec Pack'
    install standard version 
    run the .exe file 
    use default settings till installation finishes.

4-> use the combined.mp4 video to check the functionality of the video editor whenever needed





these commands willbe used later

pyinstaller --onefile -windowed --icon=player.ico